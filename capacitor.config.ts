import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'google-photorealistic-3d',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
